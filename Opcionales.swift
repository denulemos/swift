// Se usan cuando sabemos que un valor puede estar ausente, es como el default

var variable : Int? = 404 // Optional(404)

// Si quiero asignarle un NO valor a una variable, uso nil

var variableNil : Int? = 404
variableNil = nil // Ahora mi variable no posee valor

var algunMensaje : String? // Se inicializará en nil